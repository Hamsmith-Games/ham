cmake_minimum_required(VERSION 3.21 FATAL_ERROR)

project(
	{{ham_project_name}}
	VERSION "{{ham_project_version}}"
	DESCRIPTION "{{ham_project_description}}"
	LANGUAGES C CXX
)

find_package(HamEngine CONFIG REQUIRED)

add_subdirectory(src)
